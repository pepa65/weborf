### Installation 0.13.5b

test -f Makefile || {  # If Makefile is not yet generated, do:
	autoreconf -fi
	./configure
}

make maintainer-clean

## Edit to change the version:
: configure.ac
: README.md
: CHANGELOG
: weborf.1
: weborf.conf.5
: qweborf.setup.py
: qweborf/qweborf.py

autoreconf -fi

./configure

make

## Recommended:
make check

## With elevated permissions if required:
#sudo make install

## Limited install (binary and man page):
sudo cp weborf /usr/local/bin/
sudo mkdir -p /usr/local/share/man/man1
sudo cp weborf.1 /usr/local/share/man/man1/

## In order to uninstall, might require elevated permission:
#sudo make uninstall

## Uninstall limited installation:
sudo rm -- /usr/local/bin/weborf
sudo rm -- /usr/local/share/man/man1/weborf.1
